name: Copilot Setup Steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Verify runner environment
        shell: bash
        run: |
          echo "Using GitHub-hosted runner: $(hostname)"
          echo "Runner image: ${ImageOS:-unknown}"
